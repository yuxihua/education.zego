/* =========================================================
   Zego Education – Application Configuration
   =========================================================
   1. Sign up at https://console.zegocloud.com/
   2. Create a project to get your AppID and ServerSecret.
   3. Replace the placeholder values below.

   ⚠️  WARNING: Do NOT expose your ServerSecret in production.
       Generate Kit Tokens on your server and serve them via
       a secure API endpoint.
   ========================================================= */

// eslint-disable-next-line no-unused-vars
var ZEGO_APP_ID        = 0;           // replace with your numeric App ID
// eslint-disable-next-line no-unused-vars
var ZEGO_SERVER_SECRET = '';          // replace with your 32-char Server Secret
